╔═══════════════════════════════════════════════════════════════╗
║          🔐 SISTEMA DE LOGIN - INSPECTOR DIGITAL             ║
╚═══════════════════════════════════════════════════════════════╝

📁 ESTRUCTURA DEL PROYECTO
├── 📄 login.html                    ← Página de login/registro
├── 📄 Index.html                    ← Página principal (actualizada)
├── 📄 app.js                        ← Backend con autenticación
├── 📄 db.js                         ← Conexión a base de datos
├── 📄 package.json                  ← Dependencias actualizadas
├── 📄 style.css                     ← Estilos (incluye login)
├── 📄 .env.example                  ← Template de variables
│
├── 📂 public/
│   ├── 📄 script.js                 ← Scripts generales + sesión
│   └── 📄 login.js                  ← Lógica del login
│
├── 📂 ANEXOS/
│   ├── 📄 script.sql                ← BD con tabla Usuario
│   └── 📄 login-setup.sql           ← Configuración adicional
│
└── 📂 Documentación/
    ├── 📄 LOGIN_README.md           ← Documentación completa
    └── 📄 GUIA_RAPIDA_LOGIN.md      ← Esta guía rápida

═══════════════════════════════════════════════════════════════

🎯 FLUJO DE AUTENTICACIÓN

┌─────────────┐
│   Usuario   │
└──────┬──────┘
       │
       ▼
┌─────────────────────────┐
│   Visita login.html     │
└──────────┬──────────────┘
           │
           ▼
    ┌──────────────┐
    │ ¿Registrado? │
    └──┬────────┬──┘
       │        │
    NO │        │ SÍ
       │        │
       ▼        ▼
┌────────┐   ┌────────┐
│Registro│   │ Login  │
└───┬────┘   └───┬────┘
    │            │
    └─────┬──────┘
          ▼
  ┌─────────────────┐
  │ POST /auth/...  │
  │   (Backend)     │
  └────────┬────────┘
           │
           ▼
  ┌─────────────────┐
  │ Genera JWT      │
  │ + Cookie        │
  └────────┬────────┘
           │
           ▼
  ┌─────────────────┐
  │  Index.html     │
  │ (Autenticado)   │
  └─────────────────┘

═══════════════════════════════════════════════════════════════

🔒 ENDPOINTS DE AUTENTICACIÓN

┌─────────────────────────────────────────────────────────────┐
│  POST /api/auth/register                                    │
│  ────────────────────────────────────────────────────────   │
│  Body: {                                                    │
│    username, email, password, fullName, perfilId           │
│  }                                                          │
│  Response: { message, user }                               │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│  POST /api/auth/login                                       │
│  ────────────────────────────────────────────────────────   │
│  Body: {                                                    │
│    username, password                                      │
│  }                                                          │
│  Response: { message, token, user }                        │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│  GET /api/auth/me                                           │
│  ────────────────────────────────────────────────────────   │
│  Headers: Authorization: Bearer {token}                    │
│  Response: { user }                                        │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│  POST /api/auth/logout                                      │
│  ────────────────────────────────────────────────────────   │
│  Response: { message }                                     │
└─────────────────────────────────────────────────────────────┘

═══════════════════════════════════════════════════════════════

📦 DEPENDENCIAS NUEVAS

  ✓ bcryptjs         → Encriptación de contraseñas
  ✓ jsonwebtoken     → Generación y verificación de JWT
  ✓ cookie-parser    → Manejo de cookies en Express

═══════════════════════════════════════════════════════════════

🗄️ TABLA USUARIO (SQL)

┌──────────────────┬─────────────────────┬──────────────┐
│ Campo            │ Tipo                │ Descripción  │
├──────────────────┼─────────────────────┼──────────────┤
│ IDUsuario        │ INT (PK, Identity)  │ ID único     │
│ NombreUsuario    │ VARCHAR(50) UNIQUE  │ Username     │
│ Email            │ VARCHAR(100) UNIQUE │ Email        │
│ Password         │ VARCHAR(255)        │ Hash bcrypt  │
│ NombreCompleto   │ VARCHAR(150)        │ Nombre full  │
│ IDPerfil         │ INT (FK)            │ Perfil       │
│ Activo           │ BIT                 │ Estado       │
│ FechaCreacion    │ DATETIME            │ Registro     │
│ UltimoAcceso     │ DATETIME            │ Último login │
└──────────────────┴─────────────────────┴──────────────┘

═══════════════════════════════════════════════════════════════

⚡ INICIO RÁPIDO

  1. npm install
  2. Ejecutar ANEXOS/script.sql en tu base de datos
  3. npm start
  4. Abrir http://localhost:3000/login.html
  5. Registrarse e iniciar sesión

═══════════════════════════════════════════════════════════════

🛡️ PROTEGER RUTAS

  // Sin protección
  app.get('/api/datos', async (req, res) => {
    // Acceso público
  });

  // Con protección
  app.get('/api/datos', authenticateToken, async (req, res) => {
    // Solo usuarios autenticados
    // req.user tiene datos del usuario
  });

═══════════════════════════════════════════════════════════════

✨ CARACTERÍSTICAS

  ✓ Registro de usuarios
  ✓ Login con email o usuario
  ✓ Contraseñas encriptadas
  ✓ JWT con expiración 24h
  ✓ Sesión persistente
  ✓ Verificación automática de sesión
  ✓ Botón de logout
  ✓ Diseño responsive
  ✓ Validaciones frontend/backend
  ✓ Medidor de fortaleza de contraseña
  ✓ Mensajes de error claros
  ✓ Protección de rutas

═══════════════════════════════════════════════════════════════

📚 MÁS INFORMACIÓN

  → LOGIN_README.md          (Documentación completa)
  → GUIA_RAPIDA_LOGIN.md     (Guía paso a paso)
  → ANEXOS/login-setup.sql   (Consultas útiles)

═══════════════════════════════════════════════════════════════

                    🎉 ¡Sistema Listo para Usar! 🎉

═══════════════════════════════════════════════════════════════
